{
  "name": "hexperience-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Platform for generating projects from templates (CLI + Engine + Catalog + Templates)",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "test:coverage": "c8 pnpm test",
    "test:coverage:html": "c8 --reporter=html pnpm test",
    "docs": "turbo docs",
    "clean": "turbo clean",
    "check": "biome check .",
    "format:write": "biome format --write .",
    "lint:write": "biome lint --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "@evilmartians/lefthook": "^2.0.12",
    "c8": "^8.0.1",
    "typedoc": "^0.26.11",
    "turbo": "^2.6.3"
  },
  "engines": {
    "node": ">=18.12.0",
    "pnpm": ">=9.0.0"
  }
}
